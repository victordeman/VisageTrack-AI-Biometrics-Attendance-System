[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dlib"
version = "19.24.99"
description = "Shim for dlib-bin"
dependencies = ["dlib-bin==20.0.0"]
